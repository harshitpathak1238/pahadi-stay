'use client';
import Image from 'next/image'; import Link from 'next/link'; import { MapPin, Star } from 'lucide-react'; import type { Listing } from '@/lib/mock-data'; import { Badge } from './Badge';
export function ListingCard({listing}:{listing:Listing}) { return <Link href={`/${listing.category==='stay'?'stays':listing.category+'s'}/${listing.slug}`} className="image-card group block" onMouseMove={(event)=>{const bounds=event.currentTarget.getBoundingClientRect(); const x=(event.clientX-bounds.left)/bounds.width-.5; const y=(event.clientY-bounds.top)/bounds.height-.5; event.currentTarget.style.setProperty('--tilt-x',`${y * -5}deg`); event.currentTarget.style.setProperty('--tilt-y',`${x * 5}deg`);}} onMouseLeave={(event)=>{event.currentTarget.style.setProperty('--tilt-x','0deg'); event.currentTarget.style.setProperty('--tilt-y','0deg');}}><div className="relative aspect-[4/3] overflow-hidden rounded-2xl bg-[#dfe4da]"><Image src={listing.image} alt={listing.title} fill sizes="(max-width: 768px) 92vw, 30vw" className="card-image object-cover"/><span className="absolute left-3 top-3"><Badge tone="rating"><Star size={13} fill="currentColor"/> {listing.rating}</Badge></span></div><div className="pt-4"><div className="flex items-start justify-between gap-3"><div><h3 className="text-xl">{listing.title}</h3><p className="sans mt-1 flex items-center gap-1 text-sm text-[#6c7770]"><MapPin size={14}/> {listing.location}</p></div><p className="sans whitespace-nowrap text-base font-bold text-[#173f35]">₹{listing.price.toLocaleString('en-IN')}<span className="font-normal text-[#6c7770]"> / night</span></p></div></div></Link>; }
