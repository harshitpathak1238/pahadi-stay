import Link from 'next/link';
export function Button({children, href, variant='solid', type='button'}:{children:React.ReactNode; href?:string; variant?:'solid'|'outline'|'quiet'; type?:'button'|'submit'}) { const className=`sans cta-depth inline-flex items-center justify-center rounded-full px-5 py-3 text-sm font-semibold transition ${variant==='solid'?'bg-[#b66b45] text-white hover:bg-[#9f5938]':variant==='outline'?'border border-[#b9c1b8] text-[#173f35] hover:bg-white':'text-[#173f35] hover:bg-white/70'}`; return href ? <Link href={href} className={className}>{children}</Link> : <button type={type} className={className}>{children}</button>; }
