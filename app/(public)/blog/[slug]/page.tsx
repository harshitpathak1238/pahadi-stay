import type { Metadata } from 'next'; import { notFound } from 'next/navigation'; import { blogArticles } from '@/lib/blog-data'; export function generateStaticParams(){return blogArticles.map((article)=>({slug:article.slug}))} export function generateMetadata({params}:{params:{slug:string}}):Metadata{const article=blogArticles.find((item)=>item.slug===params.slug); return {title:article?.title??'Kumaon guide',description:article?.excerpt,keywords:article?.keyword}} export default function Article({params}:{params:{slug:string}}){const article=blogArticles.find((item)=>item.slug===params.slug); if(!article)notFound(); return <article className="mx-auto max-w-3xl px-5 py-14 md:py-20"><p className="sans text-xs font-bold uppercase tracking-[.2em] text-[#b66b45]">{article.category} · {article.readTime}</p><h1 className="mt-4 text-5xl leading-[1.02] text-[#173f35] md:text-6xl">{article.title}</h1><p className="mt-8 text-xl leading-8 text-[#526057]">{article.excerpt}</p><div className="mt-12 space-y-7 sans text-base leading-8 text-[#526057]">{article.content.map((paragraph)=><p key={paragraph}>{paragraph}</p>)}</div></article>}
