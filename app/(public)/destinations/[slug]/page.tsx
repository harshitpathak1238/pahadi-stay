import type { Metadata } from 'next'; import { destinations } from '@/lib/mock-data'; export function generateStaticParams(){return destinations.map(item=>({slug:item.slug}))} export function generateMetadata({params}:{params:{slug:string}}):Metadata{return {title:params.slug.replace('-', ' '),description:'Plan a thoughtful trip around Kumaon with Pahadi Stay.'}} export default function Destination({params}:{params:{slug:string}}){return <div className="mx-auto max-w-5xl px-5 py-20"><p className="sans text-xs uppercase tracking-[.2em] text-[#b66b45]">Kumaon, Uttarakhand</p><h1 className="mt-3 text-6xl capitalize">{params.slug.replace('-', ' ')}</h1><p className="mt-7 max-w-2xl text-xl leading-8 text-[#526057]">Come for the view. Stay for the slower mornings, kind people, and roads that invite you to take your time.</p></div>}
