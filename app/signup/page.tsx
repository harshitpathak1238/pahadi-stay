import { AuthForm } from '@/components/auth/AuthForm'; export const metadata={title:'Create account',description:'Create your Pahadi Stay account.'}; export default function Signup({ searchParams }: { searchParams: { email?: string } }){return <div className="mx-auto max-w-md px-5 py-16 md:py-24"><AuthForm initialMode="signup" initialEmail={searchParams.email ?? ''}/></div>}
