import { NextResponse } from 'next/server'; import { bookingSchema } from '@/lib/validations/booking'; export async function POST(request:Request){const result=bookingSchema.safeParse(await request.json()); if(!result.success)return NextResponse.json({error:result.error.flatten()},{status:400}); return NextResponse.json({bookingId:'pending-demo',message:'Booking validated. Create a Razorpay order before collecting payment.'},{status:201})}
