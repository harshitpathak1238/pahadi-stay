import { z } from 'zod'; export const bookingSchema=z.object({listingId:z.string().min(1),checkIn:z.coerce.date(),checkOut:z.coerce.date(),guests:z.coerce.number().int().min(1).max(20),guestName:z.string().min(2),guestEmail:z.string().email(),guestPhone:z.string().min(10)}).refine((value)=>value.checkOut>value.checkIn,{message:'Check-out must be after check-in',path:['checkOut']}); export type BookingInput=z.infer<typeof bookingSchema>;
